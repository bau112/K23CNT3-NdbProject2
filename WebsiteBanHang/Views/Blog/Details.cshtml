@model WebsiteBanHang.Models.BlogPost
<div class="container mt-4">
    <h2>@Model.Title</h2>
    <img src="~/images/@Model.Image" class="img-fluid mb-3" />
    <p>@Model.Content</p>
    <small><i>Ngày đăng: @Model.CreatedAt:dd/MM/yyyy</i></small>
</div>
